var numero = parseFloat(prompt("Digite um número:"));

if (numero >=10 && numero<=20 ) {
    window.confirm("Número está dentro do intervalo");
}
 else 
        window.confirm ("Número não está dentro intervalo");